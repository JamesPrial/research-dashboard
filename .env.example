# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# If you have a Max plan API key, set it here (takes priority over ANTHROPIC_API_KEY):
# MAX_API_KEY=

# Server port (default: 8420)
# PORT=8420

# User/Group ID for file permissions inside the container.
# Unraid users: set to 99/100 (the default nobody:users).
# Linux users: run `id` to find your UID/GID.
# PUID=99
# PGID=100
